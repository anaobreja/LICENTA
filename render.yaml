services:
  - type: web
    name: railway-identity
    runtime: python
    buildCommand: pip install -r requirements.txt && cd frontend && npm install && npm run build
    startCommand: python start_render.py
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./railway_demo.db
      - key: SECRET_KEY
        generateValue: true
